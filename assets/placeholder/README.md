# Placeholder art

正式美术已落地：角色为像素帧动画（`assets/characters/`，CharFrames 目录约定），
环境为 Gothicvania 裁切件（`assets/env/`）。ColorRect / Polygon2D 色块如今只作为
**headless / 素材缺失时的兜底回退**保留在场景里，运行时会被隐藏。
色板与导入规则（Nearest、无 mipmaps）见 `docs/art-spec.md`。
